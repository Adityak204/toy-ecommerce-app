apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: ecommerce
  labels:
    app: nginx-gateway
spec:
  type: NodePort
  selector:
    app: nginx-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
      name: http
      